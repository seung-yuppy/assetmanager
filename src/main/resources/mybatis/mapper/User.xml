<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.example.assetmanager.dao.UserDAO">
	<insert id="userJoin">
		INSERT INTO "User"
			(department_id, emp_no, password, username, email, phone, role, profile_image)
		VALUES (
			#{user.departmentId},
			#{user.empNo},
			#{user.password},
			#{user.username},
			#{user.email},
			#{user.phone},
			#{user.role},
			#{user.profileImage}
		)
	</insert>
	<select id="userLogin">
		SELECT *
		FROM "User"
		WHERE emp_no = #{empNo}
	</select>
	<select id="checkEmpno">
		SELECT COUNT(*)
		FROM "User"
		WHERE emp_no = #{empNo}
	</select>
	<select id="getUserInfo">
		SELECT *
		FROM "User"
		INNER JOIN Department ON Department.id = "User".department_id
		WHERE "User".id = #{userId}
	</select>
	<select id="getAdminInfo">
		SELECT *
		FROM "User"
		WHERE id = #{userId}
	</select>
</mapper>