<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.example.assetmanager.dao.UserDAO">
	<insert id="userJoin">
		INSERT INTO "User"
			(department_id, emp_no, password, username, email, phone, role)
		VALUES (
			#{user.departmentId},
			#{user.empNo},
			#{user.password},
			#{user.username},
			#{user.email},
			#{user.phone},
			#{user.role}
		)
	</insert>
	<select id="userLogin">
		SELECT *
		FROM "User"
		WHERE emp_no = #{empNo}
	</select>
	
</mapper>